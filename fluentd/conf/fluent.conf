<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match *.**>
  @type elasticsearch
  host elasticsearch
  port 9200
  user elastic
  password changeme
  index_name fluentTest
  include_timestamp true
</match>